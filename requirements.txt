# Core ADK framework
google-adk>=2.3.0

# MCP (Model Context Protocol) runtime and FastMCP server library
mcp>=1.28.0
fastmcp>=3.4.0

# Data validation (used across agents and MCP tool schemas)
pydantic>=2.0.0

# Environment variable management
python-dotenv>=1.0.0
